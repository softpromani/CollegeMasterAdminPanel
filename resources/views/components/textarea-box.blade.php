<div>
    <label class="form-label fw-semibold">
        {{ $label }}
    </label>

    <textarea
        name="{{ $name }}"
        rows="4"
        class="form-control custom-textarea"
        placeholder="{{ $placeholder }}"
    ></textarea>
</div>
